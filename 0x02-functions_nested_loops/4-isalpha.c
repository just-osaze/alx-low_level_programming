#include "main.h"

/**
  * _isalpha - prints uppercase characters
  * Return: returns uppercase characters
  * @c: variable
  *
  */

int _isalpha(in c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}

	return (0);
}
